<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/splash_overlay"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#090014"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center"
    android:visibility="gone">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvSplashAppName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <androidx.cardview.widget.CardView
            android:layout_width="160dp"
            android:layout_height="160dp"
            app:cardCornerRadius="20dp"
            app:cardBackgroundColor="#090014"
            app:cardElevation="0dp"
            android:layout_marginBottom="16dp">

            <ImageView
                android:id="@+id/ivSplashLogo"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@drawable/splash_logo"
                android:scaleType="fitCenter"
                android:alpha="0" /> </androidx.cardview.widget.CardView>

        <TextView
            android:id="@+id/tvSplashJargon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="RAKIT PC IMPIANMU TANPA BATAS"
            android:textColor="#C77DFF"
            android:textSize="14sp"
            android:textStyle="normal"
            android:letterSpacing="0.1"
            android:layout_marginTop="20dp"
            android:textAlignment="center"
            android:alpha="0" />

    </LinearLayout>
</RelativeLayout>